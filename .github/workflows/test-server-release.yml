name: AIWC Test server release

on:
  push:
    branches:
      - 'main'

jobs:
  build:
    name: Build 🛠️
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@main
      - name: Install Dependencies
        run: npm install
      - name: Making Build of Project
        run: npm run build
      - name: Archive staging artifact
        uses: actions/upload-artifact@main
        with:
          name: build
          path: build/
      
  deploy:
    name: Deploy 🛫
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@main
      - name: Download artifact
        uses: actions/download-artifact@main
        with:
          name: build
          path: build/      
      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@4.3.3
        with:
          server: ftp.codinghood.in	
          username: u589244378.aiwctest
          password: Sub@2019
          local-dir: build/
          server-dir: ./
